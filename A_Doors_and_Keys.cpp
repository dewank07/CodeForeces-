#include<bits/stdc++.h>
using namespace std;
int main() {

    int t;
    cin >> t;
    while(t--)
    {
        string a;
        cin >> a;
        int f=0;
            if(a[0]>'a' && a[i]<'z')

        for(int i = 0 ; i < a.length();i++)
        {
            if(a[i]>'a' && a[i]<'z')
            {
               f++; 
            }
            if(a[i]>'A' && a[i]<'Z')
            {

            }
            
        }

        
    }
return 0;
}



